# CPU runtime dependencies.
# The +cpu local-version pin guarantees that pip selects the CPU-only PyTorch
# wheel from the official PyTorch index instead of a CUDA-enabled build.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.9.1+cpu
transformers==5.14.1
sentence-transformers==5.6.1

grpcio==1.83.0
grpcio-tools==1.83.0
numpy>=2.0,<3
protobuf==7.35.1

# Dev / testing
pytest>=8.3,<9
pytest-asyncio>=0.25,<2
