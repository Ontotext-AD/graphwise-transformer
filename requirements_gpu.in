# GPU runtime dependencies.
# Keep PyPI as the primary index and expose the official PyTorch CUDA 12.8
# wheel repository as an extra index. The +cu128 local-version pin makes the
# selected Torch build unambiguous.
--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.9.1+cu128
transformers==5.14.1
sentence-transformers==5.6.1

grpcio==1.83.0
grpcio-tools==1.83.0
numpy>=2.0,<3
protobuf==7.35.1

# Official upstream FlashAttention 2.8.3 wheel for Linux x86_64, CPython 3.12,
# Torch 2.9.x / CUDA 12.x / CXX11 ABI. The release asset is hash-pinned.
flash-attn @ https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/flash_attn-2.8.3+cu12torch2.9cxx11abiTRUE-cp312-cp312-linux_x86_64.whl#sha256=4e2f9e39313266b1544b68138b15b91ee6221eccf14f7902b7c6620351340810

# Dev / testing
pytest>=8.3,<9
pytest-asyncio>=0.25,<2
